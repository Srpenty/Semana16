@{
    ViewData["Title"] = "Practica";
}
<ul>
    @for (int i = 0;1 <(int)ViewData["numTimes"]; i++)
            {
    <li>
        @ViewData["nombre"],
        @ViewData["edad"],
        @ViewData["añoNacimiento"]
    </li>
            }
</ul>